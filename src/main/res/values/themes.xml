<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal -->
    <style name="Theme.AppFutbol" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">#111111</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>

        <item name="android:colorBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Overlay para DatePicker con botones blancos y fondo claro -->
    <style name="ThemeOverlay.DatePicker.WhiteButtons" parent="ThemeOverlay.Material3.MaterialCalendar">
        <item name="android:forceDarkAllowed">false</item>   <!-- Forzar modo claro -->
        <item name="colorPrimary">@color/white</item>  <!-- Header morado -->
        <item name="colorOnPrimary">@color/white</item>     <!-- Texto header y botones -->
        <item name="colorSurface">@color/white</item>       <!-- Fondo calendario -->
        <item name="colorOnSurface">@color/white</item>     <!-- Texto días calendario -->
    </style>

</resources>
